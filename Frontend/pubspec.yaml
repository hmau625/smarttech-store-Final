name: smarttech_store
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  jwt_decoder: ^2.0.1
  shared_preferences: ^2.2.3
  image_picker: ^1.0.7
  country_state_city_pro: ^0.0.6
  web_socket_channel: ^2.4.5
  mime: ^1.0.6
  http_parser: ^4.0.2
  cupertino_icons: ^1.0.8
  file_picker: ^6.1.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/fondo_SmarTech.jpeg